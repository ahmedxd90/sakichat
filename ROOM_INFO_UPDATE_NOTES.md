# تحديث صفحة معلومات الغرفة — 2026-08-14

## التصميم

تمت إعادة بناء `src/components/room/RoomInfoSheet.tsx` ليطابق الكود المرفق: قائمة منزلقة بيضاء من الأسفل، خلفية معتمة مع blur، مقبض سحب، خط Cairo، تبويبا **البيانات** و**عضو**، بطاقة الغرفة، شريط المستوى، قائمة البيانات، وبطاقة عضوية بلون teal/amber.

## صلاحيات العرض

واجهة المالك تستخدم `isOwner` لإظهار زر إعدادات الغرفة فقط، وتخفي زر **متابعة** وزر **انضمام عضو** بالكامل. واجهة العضو/الزائر تعرض زر المتابعة وزر الانضمام فقط ولا تعرض إعدادات الغرفة.

## المتابعة

تمت إضافة جدول `roomFollowers` ودوال `toggleRoomFollow` و`getRoomFollowStatus` في `convex/roomSocial.ts`. الاختبار الحي نجح: الضغط على متابعة غيّر الزر إلى «متابَع» وارتفع عدد المتابعين من 0 إلى 1.

## العضوية

تمت إضافة الحقول `isPaidMember` و`membershipJoinedAt` و`membershipPricePaid` إلى `roomMembers`، وإضافة سجل `roomMembershipPayments`. تمت إضافة `purchaseRoomMembership` و`getRoomMembershipStatus` في `convex/rooms.ts`.

عند العضوية المدفوعة يتم التحقق من الرصيد، خصم السعر من العضو، إضافة السعر إلى رصيد مالك الغرفة، حفظ حالة العضوية، وتسجيل العملية. العضوية المجانية لا تخصم شيئاً. أعضاء الغرفة الحاليون لا يُطردون عند تغيير السعر، لكن أذن المايك «الأعضاء فقط» تتحقق من `isPaidMember` عند الصعود للمقعد.

الاختبار الحي للعضوية المجانية نجح: ظهرت نافذة التأكيد، تم تنفيذ الانضمام، ظهر إشعار النجاح، وتغير الزر إلى «عضو بالفعل». تبويب «عضو» عرض حالة العضوية ورصيد المستخدم.

## النشر والتحقق

تم نشر Convex فعلياً إلى `https://tremendous-eel-230.convex.cloud` بعد اكتشاف أن codegen وحده لم ينشر الدوال العامة. نجح فحص TypeScript والبناء الكامل.

رابط الاختبار: https://5173-icvoxe0vnqt4ducoecft5-9792909c.us4.manus.computer/?room-info-v2=1786709000


## تحديث إعدادات ومعلومات الغرفة — 2026-08-14

تم حذف الحقول المطلوبة من RoomInfoSheet: القلب، عرض مستوى/نقاط الغرفة، إذن المايك، عدد المقاعد، الإشعار العام، بطاقة العضوية الصفراء، حالة القفل، عرض المشرفين والمتابعين داخل البيانات، وأي عناصر زائدة من بطاقة معلومات الغرفة. تبويب الأعضاء يعرض المالك أولاً ثم المشرفين ثم الأعضاء الحقيقيين من roomMembers مع الاسم وSaki ID والوسم العضوي فقط.

تم تقييد زر إعدادات الغرفة على المالك فقط في RoomInfoSheet وRoomHeader وHardwareBackExitSheet. أزرار المتابعة والانضمام تبقى للزائر، وتختفي عن المالك.

تمت إضافة صفحة المكافأة إلى RoomSettingsPage بنفس النمط الأبيض/الفاخر للكود المرفق: رصيد المالك، مكافأة اليوم، مكافأة الأمس مع زر استلام، جدول النسب، تفعيل/تعطيل، ونسب الهدايا والعضوية ورقم الحظ.

تمت إضافة roomRewardSettings وroomRewardClaims إلى Convex. يحسب getRoomRewardStatus النقاط من giftEvents وroomMembershipPayments، وتقوم claimRoomReward بإضافة المكافأة إلى رصيد مالك الغرفة مرة واحدة فقط عن اليوم السابق. updateRoomRewardSettings لا يقبل إلا مالك الغرفة. تم نشر Convex إلى tremendous-eel-230.

الاختبار الحي: دخلت الغرفة الرسمية دون شاشة سوداء، فتحت معلومات الغرفة، تحققت من غياب الحقول المحذوفة، وفتحت تبويب الأعضاء الذي أظهر المالك ثم الأعضاء الحقيقيين. جلسة الاختبار لم تكن مالكة للغرفة، لذلك لم يظهر زر الإعدادات كما هو مطلوب. فحص TypeScript والبناء نجحا: tsc=0 وbuild=0.

ملاحظة البناء: ظهر تحذير حجم chunk موجود مسبقاً في RoomPage، لكنه لا يمنع البناء أو التشغيل.
