# سجل إصلاح الغرفة

## التشخيص الأولي
- الضغط على المقعد يفتح `RoomSeatActionSheet`.
- المكوّن كان يستخدم `useState` من دون استيراده، ما يسبب خطأ JavaScript عند أول فتح للورقة ويظهر غالباً كشاشة سوداء.
- المكوّن كان يستدعي `api.seatInvites.sendSeatInvite` مباشرة رغم أن `convex/_generated/api.d.ts` لا يحتوي وحدة `seatInvites` حالياً؛ لذلك تم وضع fallback آمن وحجب واجهة الدعوة حتى تتوفر الدالة المولدة ويُفعّل المتغير `VITE_ENABLE_SEAT_INVITES=true`.
- تم الاحتفاظ بنسخ احتياطية:
  - `src/pages/RoomPage.tsx.backup-before-seat-fix`
  - `src/components/room/RoomSeatActionSheet.tsx.backup-before-seat-fix`
  - `src/components/room/SeatInvitePopup.tsx.backup-before-seat-fix`

## التحقق
- `pnpm build` نجح بعد التعديل.
- الخطوة التالية: اختبار فتح الغرفة، فتح مقعد فارغ ومشغول، وإظهار زر مغادرة المقعد دون شاشة سوداء.

## اختبار حي أولي
- الغرفة الرسمية فتحت بعد انتظار التحميل ولم تبقَ على شاشة سوداء.
- الضغط على زر المتصلين فتح لوحة المتصلين بشكل طبيعي، ثم أُغلقت وعادت شبكة المقاعد.
- يلزم اختبار زر مقعد من الشبكة مباشرة، لأن أرقام العناصر المتاحة في المتصفح تتغير عند فتح اللوحات.

## حالة النشر والاختبار بعد التعديلات
- تم تشغيل `CONVEX_DEPLOY_KEY=... pnpm exec convex codegen` بنجاح؛ العملية قامت بتوليد bindings ورفع الدوال إلى deployment tremendous-eel-230.
- أصبح `convex/_generated/api.d.ts` يحتوي على `seatInvites`.
- تم تفعيل `VITE_ENABLE_SEAT_INVITES=true` في `.env.local` ثم إعادة تشغيل Vite.
- فحص TypeScript للـ Convex والواجهة وبناء Vite الكامل نجح.
- بعد إعادة تشغيل Vite ظهرت جلسة متصفح فارغة مؤقتاً، ثم عادت الصفحة عند فتح URL جديد؛ السبب كان جلسة/تحميل المتصفح بعد إعادة التشغيل وليس خطأ build.
- نسخة الويب الحالية تعرض الصفحة الرئيسية وبطاقات الغرف، والاختبار التالي هو فتح الغرفة من الجلسة الجديدة ثم الضغط على مقعد؛ يجب استخدام لقطة عناصر جديدة بعد إغلاق بطاقة مكافأة المستخدم.

## التحقق النهائي — 2026-08-14

تم نشر تحديثات Convex إلى deployment `tremendous-eel-230` بنجاح بعد مزامنة مخطط البيانات القديمة، بما في ذلك `roomSeatInvites` و`seatAccessMode` وحقول إعدادات الغرفة.

تم اختبار نسخة الويب عملياً: دخول الغرفة الرسمية، فتح مقعد فارغ، أخذ المقعد، فتح ورقة المقعد، ظهور زر **مغادرة المقعد**، تنفيذ المغادرة، فتح بطاقة مستخدم جالس، ظهور بطاقة الملف الشخصي البيضاء المضغوطة، وفتح معلومات الغرفة دون شاشة سوداء.

تم تحديث `RoomInfoSheet` ليعرض أذن المايك الحقيقي (`الجميع` أو `الأعضاء فقط` أو `المشرفون فقط`) ويعرض حالة عضوية الغرفة وسعرها أو كونها مجانية. تم الاحتفاظ بصفحة `RoomSettingsPage` الفاخرة التي تحفظ اسم الغرفة والوصف وعدد المقاعد وأذن المايك وسعر العضوية والزخرفة والثيم والخلفية.

تم التحقق من `VITE_ENABLE_SEAT_INVITES=true` ومن وجود وحدة `seatInvites` في API المولّد، كما نجح TypeScript والبناء الكامل.

النسخة المؤرشفة الحالية: `/home/ubuntu/saku_project_room_updates_2026-08-14.zip`

ملاحظة الاختبار: دعوة مستخدم إلى مقعد تتطلب حساب مالك/مشرف في جلسة الاختبار، لكن الربط البرمجي موجود في `UserProfileSheet` و`RoomPage` و`RoomSeatActionSheet`، وتم نشر دوال Convex اللازمة فعلياً.
