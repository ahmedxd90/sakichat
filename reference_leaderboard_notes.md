# مواصفات مرجع لوحة المتصدرين

المصدر: الملف المرفق `pasted_content.txt`، وهو نموذج HTML عربي باتجاه RTL.

الهوية البصرية المرجعية فاتحة: خلفية `#f8fafc`، بطاقات بيضاء، نص داكن slate، حدود رمادية خفيفة، رأس ثابت مع blur، وخط Cairo بأوزان 400 و600 و700 و800 و900. الصفحة مصممة بعرض هاتف ضيق تقريبًا `max-w-md`.

الهيكل المطلوب هو رأس ثابت يحتوي ثلاثة تبويبات رئيسية: الثروة، الكاريزما، والغرف. أسفله مباشرة مرشح زمني بثلاثة خيارات: يومي، أسبوعي، شهري. منطقة المحتوى تعرض منصة للمراكز الثلاثة الأولى، ثم قائمة المراكز من الرابع فما بعد. يوجد في الأسفل شريط ثابت يعرض ترتيب المستخدم الحالي وصورته واسمَه وPRO ودرجته وقيمة تقدمه.

التطبيق الحقيقي يجب أن يستبدل بيانات النموذج الثابتة باستعلامات Convex: `leaderboards.getWealthLeaderboard` و`leaderboards.getCharismaLeaderboard` و`leaderboards.getRoomsLeaderboard` مع فترات `daily` و`weekly` و`monthly`. الضغط على مستخدم يفتح ملفه، والضغط على غرفة يفتح الغرفة.

الطلبات الخاصة بالمستخدم: فتح الصفحة من زر الكأس الثابت في HomePage، دعم الثروة والكاريزما والغرف، عرض ترتيب حقيقي للمستخدمين والغرف، والحفاظ على نمط المرجع في الألوان والخطوط والهوامش والترتيب.
