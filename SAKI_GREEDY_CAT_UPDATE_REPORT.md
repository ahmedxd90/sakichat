# تحديث لعبة «القط الشجع» داخل Saki

تم تحديث لعبة «القط الشجع» الموجودة داخل تطبيق Saki نفسه، مع الحفاظ على اسمها العربي ومنطقها الحالي المبني على Convex. لم يتم استبدال اللعبة بل تم إعادة تنسيق واجهتها فوق وظائفها الحالية.

## ما تم تحديثه

أصبحت الواجهة تستخدم أسلوب عجلة نيون مستوحى من الفيديو المرجعي، مع خلفية فضائية داكنة، نقاط ضوء، توهج بنفسجي وذهبي، مؤشر علوي، لوحة مركزية، وهوية القط. بقيت عناصر الطعام الثمانية، معاملات الفوز، مبالغ الرهان، المؤقت، رهانات المستخدم، الرهانات المباشرة، آخر الجولات، النتائج، المتصدرون والقواعد كما هي ومتصلة بنفس دوال Convex.

تم الحفاظ على اسم اللعبة «القط الشجع» في العنوان والواجهة. كما تم الحفاظ على وظائف `api.greedyCat.*`، بما فيها إنشاء الجولة، وضع الرهان، احتساب النتيجة، التوزيع، سجل النتائج، المتصدرين وإعادة التشغيل.

## نتائج التحقق

نجح فحص TypeScript باستخدام `pnpm exec tsc --noEmit`. ونجح بناء نسخة الويب باستخدام `pnpm run build`. كما نجحت مزامنة Capacitor وبناء `assembleRelease` و`bundleRelease` داخل Android.

## المخرجات

- نسخة الويب: `dist/`
- APK: `android/app/build/outputs/apk/release/app-release.apk`
- AAB: `android/app/build/outputs/bundle/release/app-release.aab`

هذه النسخة تعد تحديثًا داخل مشروع Saki الأصلي وليست مشروعًا منفصلًا.
