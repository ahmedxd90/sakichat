# DJ Spin — البنية

## طبقات التنفيذ

- `src/pages/RoomPage.tsx`: نقطة فتح اللعبة من قائمة الألعاب وتمرير roomId وبيانات المستخدم.
- `src/components/games/DJSpinGame.tsx`: واجهة اللوحة المنزلقة، الرموز، الرهان، الدوران، القواعد والسجل.
- `src/components/games/djSpinConfig.ts`: تعريف الرموز والمضاعفات ومبالغ العرض.
- `src/lib/djSpinDemo.ts`: محرك الجولة التجريبية المحلي، لا يخصم عملات حقيقية.
- `convex/djSpin.ts`: المرحلة الخادمية اللاحقة لإنشاء الجولة، قفل الرهان، RNG، والتسوية الذرية.
- `convex/schema.ts`: جداول الجولات والرهانات والمعاملات بعد اعتماد المخطط.

## مبدأ الأمان

واجهة اللعبة تعرض الحالة فقط. عند تفعيل الاقتصاد الحقيقي، لا تقبل Convex نتيجة أو ربحًا قادمًا من العميل؛ الخادم وحده ينشئ النتيجة ويحسبها ويسجلها.

## حالات الجولة

`betting` ثم `spinning` ثم `settled`، مع `cancelled` عند فشل الخادم أو انتهاء الجلسة. لكل جولة `roomId`, `roundId`, `seed/nonce` خادمي، ووقت إغلاق واضح.
