import React from "react";

/**
 * Gift delivery remains fully active in Convex. This component intentionally
 * renders no floating/global gift banner; room gift sheets and gift history
 * continue to work normally without showing a flying strip.
 */
export default function GlobalGiftBanner() {
  return null;
}
