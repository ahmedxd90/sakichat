# سجل إصلاح شاشة الغرفة وإخفاء الريلز

التاريخ: 2026-08-14

## السبب المؤكد
كان مكوّن `SeatInvitePopup` يستدعي `seatInvites:getMyPendingInvite` بينما الدالة غير منشورة في بيئة Convex الحالية (`tremendous-eel-230`). أدى خطأ Convex غير المعالج إلى تفريغ `root` بعد انتهاء حاجز دخول الغرفة، فظهرت الشاشة السوداء.

## الإصلاح
تمت حماية عرض `SeatInvitePopup` خلف المتغير `VITE_ENABLE_SEAT_INVITES`، وضبط المتغير في `.env.local` إلى `false` مؤقتاً حتى يتم نشر `convex/seatInvites.ts` إلى نفس البيئة. لم تُحذف الميزة أو ملفاتها، ويمكن إعادة تفعيلها بعد النشر.

## نتيجة الاختبار
بعد إعادة البناء والتشغيل، تم الدخول إلى غرفة «الدعم الفني والاستفسارات» بنجاح. ظهرت الخلفية والمقاعد والدردشة وأزرار الغرفة، وبقي `root` ممتلئاً (`rootEmpty=false`) دون الشاشة السوداء.

## الشريط السفلي
تم إخفاء تبويب الريلز من `BottomNav.tsx`. أظهر فحص الواجهة أن `reelsVisible=false`؛ بقيت تبويبات الرئيسية واللحظات والرسائل وأنا.

## ملاحظات غير مانعة
ظهر في Console خطأ صلاحية من `rooms:fixOwnerRoles` لحساب الاختبار («ليس لديك صلاحية»)، لكنه لا يمنع عرض الغرفة أو تشغيلها.

## البناء
نجح فحص TypeScript وبناء Vite. لا تُفعّل دعوات المقاعد قبل نشر وحدة Convex في بيئة `tremendous-eel-230`؛ بعد النشر يمكن ضبط `VITE_ENABLE_SEAT_INVITES=true` وإعادة البناء.
